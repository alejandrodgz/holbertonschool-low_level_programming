#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * puts_half - every other character
 * @str: pointer
 * Return: length of string
 */

void puts_half(char *str)
{
	int len;
	len = strlen(str);
	*str = len;
	while (*str != '\0')
	{
		_putchar(*str);
		str += 1;
	}
	_putchar('\n');
}
