#include "main.h"

/**
 * _abs - prints sign of number
 * @n: parameter to have in mind
 * Return: 0
 */

int _abs(int n)
{
		if (n > 0)
		{
			n = n * 1;
			return (n);
		}
		else if (n < 0)
		{
			n = n * -1;
			return (n);
		}
		else
		{
		return (n);
		}
}
