#include "main1.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * main - check code
 * 
 * Return: always 0
 */

int main (void)
{
    char *concat;

    concat = string_nconcat("best ", "School !!!", 6);
    printf("%s\n", concat);
    free(concat);
    return (0);
}