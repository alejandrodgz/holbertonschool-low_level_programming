#include <stdio.h>
#include "main.h"

/**
 * print_line- print a line with n c length
 *
 * @n: length
 * Return: character
 */

void print_line(int n)
{
	int i;

	for (i = 0; i <= n; i++)
	{
		if (n < 0)
		{
		_putchar ('\n');
		break;
		}
		_putchar (95);
	}
	_putchar ('\n');
}
