#include <stdio.h>
#include "main.h"

/**
 * mul - return the value of mul two integer
 *
 * @a: character to compute
 * @b: character to compute
 * Return: character
 */

int mul(int a, int b)
{

	return (a * b);
}
