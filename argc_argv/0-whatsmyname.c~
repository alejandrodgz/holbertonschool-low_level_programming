#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/**
 * main - Write a program that prints its name
 *
 * @argc: number of parameters
 * @argv: strings
 * Return: name of program
 */

int main(char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
