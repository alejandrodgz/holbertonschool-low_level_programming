#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char prislike[] = "Programming is like building a multilingual puzzle";
	char puts(prislike);
	return(0);
}
