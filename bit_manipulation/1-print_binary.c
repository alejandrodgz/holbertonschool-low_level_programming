#include "main.h"

 /**
  * print_binary - print binary
  * @n: number
  *
  */

void print_binary(unsigned long int n)
{
	 unsigned long int n2 = n;
	 int a = 0;

	 while (a > 0)
	 { 
		  a++;
		  a = a >> 1;
	 }
	 a--;
	 if (a > 0);



