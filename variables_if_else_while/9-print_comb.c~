#include <stdio.h>
/**
 * main - Entry point
 *
 *Return: always 0
 */

int main(void)
{
	int i, j;
	int A[i][j];

	for (i = 0; i <= 9; i++)
	{
		for (j = 0; j <= 9; j++)
			printf("%d\n", A);
	}
	return (0);
}
