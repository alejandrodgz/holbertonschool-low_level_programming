#include "main.h"

/**
 * print_last_digit - prints sign of number
 * @n: parameter to have in mind
 * Return: 0
 */

int print_last_digit(int n)
{
	int c = n % 10;

	_putchar(c + '0');
	return c + 48;
}
